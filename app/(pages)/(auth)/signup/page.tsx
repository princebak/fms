import SignupForm from "@/app/components/form/SignupForm";
import React from "react";

export const metadata = {
  title: "FMS | Signup",
};

const page = () => {
  return <SignupForm />;
};

export default page;
