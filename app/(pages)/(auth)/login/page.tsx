import React from "react";
import LoginForm from "../../../components/form/LoginForm";

export const metadata = {
  title: "FMS | Login",
};

const page = () => {
  return <LoginForm />;
};

export default page;
