import ConfimResetPwForm from "@/app/components/form/ConfirmResetPwForm";
import React from "react";

export const metadata = {
  title: "FMS | Confim reset password",
};

const page = () => {
  return <ConfimResetPwForm />;
};

export default page;
